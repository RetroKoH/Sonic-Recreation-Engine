/// @description Init bumper
// We will use a slightly different one in the bonus stage
routine=0;
hit=0;         // Bumper hit count. After 10 hits, no more points for you.

scr_init_anim(global.AnimationsBumper,0);