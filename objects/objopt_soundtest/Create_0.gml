/// @description Insert description here
enabled = false;	// If false, cannot be seen or utilized
active = false;		// IDK why we need two flags, but here we are.
track_index = 0;	// The track being viewed
max_index = BGM_tracks.total;	// Total # of BGM tracks
playing = false;	// If true, that means an audio track is being played.
